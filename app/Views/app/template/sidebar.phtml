<?php

$menuRenderer = new \App\Services\Menu\MenuRenderer();

$userName = $user->name ?? $user->nickname ?? $user->login ?? 'Usuario';
$defaultAvatar = asset('common/images/no-photo.png');
$userAvatar = !empty($user->avatar) && $user->avatar !== $defaultAvatar ? $user->avatar : null;
$logoutRoute = $router->route($auth->getRouteLogout());
$sidebarLogo = $logo_sidebar ?? $logo_light ?? asset('common/images/no-photo.png');
$sidebarMobileLogo = $logo_mobile ?? $sidebarLogo;
$sidebarLogoRoute = $layout['topbar-logo-route'] ?? '#';
$sidebarClasses = trim((string) ($sidebarClasses ?? ''));

if (is_string($sidebarLogoRoute) && $router->hasNamedRoute($sidebarLogoRoute)) {
    $sidebarLogoRoute = $router->route($sidebarLogoRoute);
}
?>

<nav id="sidebarMenu" class="sidebar bg-gray-800 text-white collapse <?= htmlspecialchars($sidebarClasses) ?>" data-simplebar data-menu-root="primary">
    <div class="sidebar-inner ps-3 pe-4 pt-3">

        <div class="sidebar-mobile-header d-flex d-lg-none align-items-center justify-content-between mb-3">
            <a href="<?= htmlspecialchars($sidebarLogoRoute) ?>" class="sidebar-mobile-brand">
                <img src="<?= htmlspecialchars($sidebarMobileLogo) ?>" alt="Logo">
            </a>

            <div class="sidebar-close">
                <a href="#sidebarMenu"
                   data-bs-toggle="collapse"
                   data-bs-target="#sidebarMenu"
                   aria-controls="sidebarMenu"
                   aria-expanded="true"
                   aria-label="Fechar menu"
                   class="text-white">

                    <svg class="icon icon-xs" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd"
                              d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z"
                              clip-rule="evenodd"></path>
                    </svg>
                </a>
            </div>
        </div>

        <?php if (!empty($layout["sidebar_mobile_usercard"])) : ?>
            <div class="user-card d-flex d-lg-none align-items-center justify-content-between pb-4 mb-2">
                <div class="d-flex align-items-center">
                    <div class="avatar-lg me-3">
                        <?php if ($userAvatar) : ?>
                            <img
                                src="<?= htmlspecialchars($userAvatar) ?>"
                                class="card-img-top rounded-circle border-white"
                                alt="<?= htmlspecialchars($userName) ?>"
                            >
                        <?php else : ?>
                            <span class="avatar rounded-circle d-inline-flex align-items-center justify-content-center w-100 h-100 bg-gray-700 text-white">
                                <i class="uil uil-user-circle fs-3"></i>
                            </span>
                        <?php endif ?>
                    </div>

                    <div class="d-block">
                        <h2 class="h6 mb-2 text-white">
                            Ola, <?= htmlspecialchars($userName) ?>
                        </h2>

                        <a href="#"
                           class="btn btn-secondary btn-sm d-inline-flex align-items-center"
                           data-logoff="<?= htmlspecialchars($logoutRoute) ?>">
                            <i class="uil uil-signout me-1"></i>
                            Sair
                        </a>
                    </div>
                </div>
            </div>
        <?php endif; ?>

        <ul class="nav flex-column pt-3 pt-md-0">
            <li class="nav-item brand">
                <a href="<?= htmlspecialchars($sidebarLogoRoute) ?>" class="sidebar-brand-link">
                    <img src="<?= htmlspecialchars($sidebarLogo) ?>" alt="Logo">
                </a>
            </li>
            <?= $menuRenderer->render($menu, 1, 'vertical') ?>
        </ul>

        <?php if (!empty($layout["sidebar_footer"])) : ?>
            <div class="sidebar-footer mt-4 pt-3 pb-2">
                <?php if (is_array($layout["sidebar_footer"])) : ?>
                    <?php if (!empty($layout["sidebar_footer"]["title"])) : ?>
                        <h6 class="sidebar-footer-title mb-2">
                            <?= htmlspecialchars($layout["sidebar_footer"]["title"]) ?>
                        </h6>
                    <?php endif; ?>

                    <?php if (!empty($layout["sidebar_footer"]["text"])) : ?>
                        <p class="sidebar-footer-text mb-3">
                            <?= htmlspecialchars($layout["sidebar_footer"]["text"]) ?>
                        </p>
                    <?php endif; ?>

                    <?php if (!empty($layout["sidebar_footer"]["button_text"])) : ?>
                        <a href="<?= htmlspecialchars($layout["sidebar_footer"]["button_link"] ?? '#') ?>"
                           class="btn btn-secondary btn-sm w-100 <?= !empty($layout["sidebar_footer"]["button_class"]) ? htmlspecialchars($layout["sidebar_footer"]["button_class"]) : '' ?>">
                            <?= htmlspecialchars($layout["sidebar_footer"]["button_text"]) ?>
                        </a>
                    <?php endif; ?>
                <?php else : ?>
                    <div class="sidebar-footer-text">
                        <?= $layout["sidebar_footer"] ?>
                    </div>
                <?php endif; ?>
            </div>
        <?php endif; ?>

    </div>
</nav>
