<?= $this->insert("app/template/head"); ?>
<title><?=TITLE_SITE?> | Esqueci minha senha</title>
<script src="<?= asset("dist/notyf/notyf.min.js") ?>"></script>
<script src="<?= asset("app/js/login.min.js") ?>" defer></script>
<?php if ($recaptcha) : ?>
<script src="https://www.google.com/recaptcha/api.js?render=<?=$recaptcha?>" defer></script>
<script>rkey="<?=$recaptcha?>"</script>
<?php endif ?>
<script>window.passwordMinLength = <?= (int)($password_min_length ?? 5) ?>;</script>
<?php if ($background) : ?>
<style type="text/css">
body {
    background: url("<?=$background?>") no-repeat center center fixed;
    -webkit-background-size: cover;
    -moz-background-size: cover;
    -o-background-size: cover;
    background-size: cover;
}
</style>
<?php endif ?>
</head>

<body>
    <div class="authentication-page<?= ($template ?? "fluid") === "fluid" ? " fluid" : "" ?>">
        <?php if (($template ?? "fluid") === "fluid") : ?>
        <div class="wrapper">
        <?php endif ?>
            <div id="login-box" class="<?= ($template ?? "fluid") === "box" ? "bg-white rounded border-light" : "" ?> border-0 p-4 p-lg-5 m-auto">

                <?php if ($logo) : ?>
                    <div class="login-logo">
                        <img src="<?=$logo?>" alt="<?= app("name") ?>" height="<?= $logo_height ?? "100" ?>">
                    </div>
                <?php endif ?>

                <div class="text-center text-md-center mb-4 mt-md-0">
                    <h1 class="mb-0 h3">Esqueci a minha senha</h1>
                </div>

                <form id="form-password-request" action="<?=$action?>" method="POST" class="mt-4" validate>
                    <input type="hidden" name="csrf" value="<?=$csrf?>">

                    <p class="text-center">Para recuperar a sua senha, digite o seu endereço de e-mail abaixo e enviaremos instruções para redefinição.</p>

                    <div class="form-group mb-4">
                        <label class="fw-medium mb-2" for="email">Seu e-mail</label>
                        <div class="input-group">
                            <span class="input-group-text bg-white" id="basic-addon1">
                                <i class="fa-solid fa-envelope text-gray-600"></i>
                            </span>
                            <input type="email" class="form-control form-control-lg border-start-0" placeholder="Digite seu e-mail cadastrado" id="email" name="email" autocomplete="email" autofocus required>
                        </div>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg">Enviar link</button>
                    </div>

                    <div class="text-center mt-3">
                        <a href="<?=$login_url?>" class="small">Voltar ao login</a>
                    </div>

                    <div id="login-message" class="mt-3"><?php flash() ?></div>
                </form>

            </div>
        <?php if (($template ?? "fluid") === "fluid") : ?>
        </div>
        <?php endif ?>
    </div>
</body>
</html>
