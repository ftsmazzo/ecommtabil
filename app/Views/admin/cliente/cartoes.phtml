<?php $this->layout("app/template/base"); ?>

<?php $this->start("endScripts") ?>
<script src="<?=asset("admin/js/cartao-form.js")?>"></script>
<?php $this->end() ?>

<?php $this->start("page-buttons") ?>
    <a class="btn btn-white" href="<?=$router->route("admin.cliente.index")?>"><i class="uil uil-left me-1"></i> Voltar</a>
    <?php if (!empty($permissao["cartao_gerenciar"])): ?>
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#modal-novo-cartao-cliente">
            <i class="uil uil-plus me-1"></i> Novo Cartão
        </button>
    <?php endif ?>
<?php $this->end() ?>

<div class="row g-3 mb-3">
    <div class="col-12 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <h5 class="card-title mb-1"><?=$cliente->principal_print?></h5>
                <div class="text-muted mb-2"><?=$cliente->documento_print?></div>
                <div class="mb-1"><strong>Telefone:</strong> <?=$cliente->telefone_print?></div>
                <?php if (!empty($cliente->ramo_descricao)): ?>
                    <div class="mb-1"><strong>Ramo:</strong> <?=$cliente->ramo_descricao?></div>
                <?php endif ?>
                <div><strong>Situação:</strong> <?=$cliente->situacao_print?></div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted mb-1">Total de cartões</div>
                <h2 class="mb-0"><?=$total_cartoes?></h2>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted mb-1">Saldo total</div>
                <h2 class="mb-0"><?=$saldo_total_print?></h2>
            </div>
        </div>
    </div>
</div>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table"
                   data-datatable-options='{"paging":false}'>
                <thead>
                    <tr>
                        <th>Cartão</th>
                        <th>Status</th>
                        <th>Saldo</th>
                        <th>Validade</th>
                        <?php if (!empty($permissao["cartao_editar"]) || !empty($permissao["cartao_gerenciar"])): ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($cartoes as $cartao): ?>
                        <tr>
                            <td>
                                <div class="cell-title">
                                    <span><?=$cartao->codigo_print?></span>
                                </div>
                            </td>
                            <td><?=$cartao->status_print?></td>
                            <td><?=$cartao->saldo_print?></td>
                            <td><?=$cartao->validade_print?></td>
                            <?php if (!empty($permissao["cartao_editar"]) || !empty($permissao["cartao_gerenciar"])): ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if (!empty($permissao["cartao_editar"])): ?>
                                            <a class="table-action-btn" data-tooltip="true" title="Editar cartão" href="<?=$cartao->abrir_editar?>"><i class="uil uil-edit-alt"></i></a>
                                        <?php endif ?>
                                        <?php if (!empty($permissao["cartao_gerenciar"])): ?>
                                            <a class="table-action-btn" data-tooltip="true" title="Abrir Detalhe" href="<?=$cartao->abrir_hub?>"><i class="uil uil-eye"></i></a>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                    <?php if (empty($cartoes)): ?>
                        <tr>
                            <td colspan="<?= (!empty($permissao["cartao_editar"]) || !empty($permissao["cartao_gerenciar"])) ? 5 : 4 ?>" class="text-center text-muted py-4">
                                Nenhum cartão vinculado a este cliente.
                            </td>
                        </tr>
                    <?php endif ?>
                </tbody>
            </table>
        </div>
    </div>
</div>

<?php if (!empty($permissao["cartao_gerenciar"])): ?>
<div class="modal fade" id="modal-novo-cartao-cliente" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form id="form-cartao-cliente" action="<?=$router->route("admin.cartao.insert")?>" method="POST" class="form-validate" data-codigo-check-url="<?=$router->route("admin.cartao.codigo.disponivel")?>" data-token-nfc-check-url="<?=$router->route("admin.cartao.token.nfc.disponivel")?>">
                <div class="modal-header">
                    <h5 class="modal-title">Novo Cartão</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" name="csrf" value="<?=$csrf?>">
                    <input type="hidden" name="id_cliente" value="<?=$cliente->id?>">
                    <input type="hidden" name="status" value="ATIVO">

                    <div class="mb-3">
                        <label for="cartao_novo_codigo" class="form-label">Código Único <span class="text-danger">*</span></label>
                        <input type="text" class="form-control input-upper" id="cartao_novo_codigo" name="codigo_unico" placeholder="000001" autocomplete="off" inputmode="numeric" required>
                        <small class="text-muted d-block mt-1">Use os 6 dígitos impressos no cartão.</small>
                    </div>

                    <div class="mb-3">
                        <label for="cartao_novo_validade" class="form-label">Validade</label>
                        <input type="date" class="form-control" id="cartao_novo_validade" name="validade" autocomplete="off">
                    </div>

                    <div class="mb-3">
                        <label for="cartao_novo_token_nfc" class="form-label">Número serial do cartão <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="cartao_novo_token_nfc" name="token_nfc" placeholder="Aproxime o cartao do leitor" autocomplete="off" required>
                        <small class="text-muted d-block mt-1">A leitura apenas preenche o campo. O cartão só será salvo ao clicar em Salvar cartão.</small>
                    </div>

                    <div class="mb-0">
                        <label for="cartao_novo_observacoes" class="form-label">Observações</label>
                        <textarea class="form-control" id="cartao_novo_observacoes" name="observacoes" rows="4" placeholder="Anotações opcionais"></textarea>
                    </div>
                </div>
                <div class="modal-footer mt-3">
                    <button type="button" class="btn btn-white" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-primary">
                        <span class="btn-text"><i class="uil uil-check me-1"></i>Salvar cartão</span>
                        <span class="btn-loading d-none"><span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>Salvando...</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
<?php endif ?>
