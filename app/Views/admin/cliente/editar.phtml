<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<?php $this->start("endScripts") ?>
<script>
    window.clienteFormConfig = {
        findUrl: "<?=$router->route("admin.cliente.find")?>",
        currentId: "<?=$cliente->id?>",
        required: <?=json_encode($required ?? "", JSON_UNESCAPED_UNICODE)?>,
        estados: <?=json_encode($estados_br ?? [], JSON_UNESCAPED_UNICODE)?>,
        pessoaFixa: <?=json_encode($pessoa_fixa ?? false)?>,
        pessoaOpcoes: <?=json_encode($pessoa_opcoes ?? ["F", "J"], JSON_UNESCAPED_UNICODE)?>
    };
</script>
<script src="<?=asset("admin/js/cliente-form.js")?>"></script>
<?php $this->end() ?>

<form id="form-cliente-editar" class="form-validate" action="<?=$router->route("admin.cliente.update")?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <input type="hidden" name="id" value="<?=$cliente->hash()?>">

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Identificação</h5>
                    <small>Informe o nome do grupo de clientes.</small>
                </div>

                <div class="row">
                    <?php /* Campos ocultos para manter compatibilidade com o banco */ ?>
                    <input type="hidden" name="pessoa" value="<?=$cliente->pessoa ?: ($pessoa_opcoes[0] ?? 'J')?>">

                    <div class="col-12 col-lg-8">
                        <div class="mb-3">
                            <label for="razao" class="form-label">Nome</label>
                            <input type="text" class="form-control" id="razao" name="razao" placeholder="Nome do grupo" autocomplete="off" value="<?=$cliente->razao?>" required>
                        </div>
                    </div>

                    <?php /*
                    CAMPOS OCULTOS - mantidos para compatibilidade futura

                    <div class="col-12 col-sm-8 col-md-5 col-lg-5">
                        CNPJ / CPF (documento) - value: <?=$cliente->documento?>
                    </div>
                    <div class="col-12 col-md-4 col-lg-4 col-xl-4">
                        IE / RG (rg_ie) - value: <?=$cliente->rg_ie?>
                    </div>
                    <div class="col-12 col-lg-6">
                        Nome Fantasia / Apelido (nome) - value: <?=$cliente->nome?>
                    </div>
                    <div class="col-12 col-sm-5 col-md-6 col-lg-4 col-xl-3">
                        Data de Abertura / Nascimento (nascimento) - value: <?=datebr($cliente->nascimento)?>
                    </div>
                    <div class="col-12 col-sm-7 col-md-6 col-lg-4 col-xl-3">
                        Contato (contato) - value: <?=$cliente->contato?>
                    </div>
                    <div class="col-12 col-sm-6 col-lg-4 col-xl-3">
                        Telefone (telefone) - value: <?=$cliente->telefone?>
                    </div>
                    <div class="col-12 col-sm-6 col-lg-4 col-xl-3">
                        WhatsApp (whatsapp) - value: <?=$cliente->whatsapp?>
                    </div>
                    <div class="col-12 col-lg-8 col-xl-6">
                        E-mail (email) - value: <?=$cliente->email?>
                    </div>
                    <div class="col-12 col-lg-12 col-xl-6">
                        Site (site) - value: <?=$cliente->site?>
                    </div>

                    SEÇÃO CLASSIFICAÇÃO:
                    Situação (id_situacao) - value: <?=$cliente->id_situacao?>
                    Ramo (id_ramo) - value: <?=$cliente->id_ramo?>

                    SEÇÃO ENDEREÇO:
                    CEP (cep) - value: <?=$cliente->cep?>
                    Logradouro (endereco) - value: <?=$cliente->endereco?>
                    Número (numero) - value: <?=$cliente->numero?>
                    Complemento (complemento) - value: <?=$cliente->complemento?>
                    Bairro (bairro) - value: <?=$cliente->bairro?>
                    Cidade (cidade) - value: <?=$cliente->cidade?>
                    Estado (estado) - value: <?=$cliente->estado?>
                    País (pais) - value: <?=$cliente->pais ?: "Brasil"?>

                    SEÇÃO OBSERVAÇÕES:
                    Observações (observacoes) - value: <?=$cliente->observacoes?>
                    */ ?>
                </div>
            </div>
            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$url_back?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>

<div class="card crud-table-card mt-4">
    <div class="card-header d-flex align-items-center justify-content-between">
        <h5 class="mb-0">Empresas</h5>
        <?php if (allow("empresa_inserir")): ?>
            <a class="btn btn-primary btn-sm" href="<?=$router->route("admin.empresa.novo", ["id_cliente" => $cliente->id])?>"><i class="fa fa-plus fa-fw"></i> Nova Empresa</a>
        <?php endif ?>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable crud-table">
                <thead>
                    <tr>
                        <th>Empresa</th>
                        <th>Documento</th>
                        <th>Telefone</th>
                        <?php if (allow("empresa_editar") || allow("empresa_excluir")): ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($empresas as $emp): ?>
                        <tr>
                            <td>
                                <?php if (allow("empresa_editar")): ?>
                                    <a href="<?=$emp->editar_url?>"><?=htmlspecialchars(trim((string) ($emp->razao ?: $emp->nome)), ENT_QUOTES, "UTF-8")?></a>
                                <?php else: ?>
                                    <span><?=htmlspecialchars(trim((string) ($emp->razao ?: $emp->nome)), ENT_QUOTES, "UTF-8")?></span>
                                <?php endif ?>
                            </td>
                            <td><?=htmlspecialchars($emp->documento_print, ENT_QUOTES, "UTF-8")?></td>
                            <td><?=$emp->telefone ? phone($emp->telefone) : "—"?></td>
                            <?php if (allow("empresa_editar") || allow("empresa_excluir")): ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if (allow("empresa_editar")): ?>
                                            <a href="<?=$emp->editar_url?>" class="table-action-btn" data-tooltip="true" title="Editar"><i class="uil-edit-alt"></i></a>
                                        <?php endif ?>
                                        <?php if (allow("empresa_excluir")): ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir" onclick="Delete('empresas/delete', '<?=$emp->id?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
