<form id="form-dre-tipo" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if ($tipo) : ?>
        <input type="hidden" name="id" value="<?=$tipo->id?>">
    <?php endif ?>

    <div class="mb-3">
        <label for="dre-tipo-nome" class="form-label">Nome <span class="text-danger">*</span></label>
        <input type="text" class="form-control" id="dre-tipo-nome" name="nome"
            placeholder="Ex: DVA — Demonstração do Valor Adicionado"
            value="<?=htmlspecialchars($tipo->nome ?? "", ENT_QUOTES, "UTF-8")?>"
            required autofocus>
    </div>

    <div class="mb-3">
        <label for="dre-tipo-sigla" class="form-label">Sigla <span class="text-danger">*</span></label>
        <input type="text" class="form-control" id="dre-tipo-sigla" name="sigla"
            placeholder="Ex: dva" maxlength="10"
            value="<?=htmlspecialchars($tipo->sigla ?? "", ENT_QUOTES, "UTF-8")?>"
            required>
        <small class="text-muted">Identificador curto e único, sem espaços (ex: DRE, DFC, BP).</small>
    </div>
</form>
