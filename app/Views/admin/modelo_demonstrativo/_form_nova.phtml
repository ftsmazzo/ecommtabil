<form id="form-modelo-demonstrativo" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <input type="hidden" name="tipo" value="<?=htmlspecialchars($tipo, ENT_QUOTES, "UTF-8")?>">

    <div class="mb-3">
        <label for="modelo-demo-nome" class="form-label">Nome <span class="text-danger">*</span></label>
        <input type="text" class="form-control" id="modelo-demo-nome" name="nome"
            placeholder="Ex: DRE Empresa XPTO" required autofocus>
    </div>

    <div class="mb-3">
        <label for="modelo-demo-empresa" class="form-label">Empresa</label>
        <select class="form-select" id="modelo-demo-empresa" name="id_empresa">
            <option value="">— Nenhuma (não recomendado) —</option>
            <?php foreach ($empresas as $empresa) : ?>
                <?php if (in_array($empresa->id, $idsComModelo, true)) continue; ?>
                <option value="<?=$empresa->id?>"><?=htmlspecialchars($empresa->razao, ENT_QUOTES, "UTF-8")?></option>
            <?php endforeach ?>
        </select>
        <small class="text-muted">Empresas que já têm um modelo não aparecem na lista.</small>
    </div>

    <div class="mb-3">
        <label for="modelo-demo-clonar" class="form-label">Clonar modelo de</label>
        <select class="form-select" id="modelo-demo-clonar" name="clonar_de_id_configuracao">
            <option value="">Não clonar — começar em branco</option>
            <?php foreach ($modelos as $m) : ?>
                <option value="<?=$m->id?>"><?=htmlspecialchars($m->nome, ENT_QUOTES, "UTF-8")?></option>
            <?php endforeach ?>
        </select>
    </div>
</form>
