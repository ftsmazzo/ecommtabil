<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-buttons") ?>
    <?php if ($permissao["inserir"]): ?>
        <button type="button" class="btn btn-primary" data-modal-ajax="<?=$router->route("admin.indice.financeiro.novo")?>" data-modal-target="#modal-indice-financeiro" data-modal-title="Novo Índice Financeiro"><i class="fa fa-plus fa-fw"></i> Novo Índice</button>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Sigla</th>
                        <th>Nome</th>
                        <th>Unidade</th>
                        <th>Último Valor</th>
                        <th>Data Ref.</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr data-record-id="<?=$d->hash?>">
                            <td>
                                <span class="badge bg-secondary"><?=$d->sigla?></span>
                            </td>
                            <td>
                                <?php if ($permissao["editar"]): ?>
                                    <a href="<?=$router->route("admin.indice.financeiro.editar", ["id" => $d->hash])?>"><?=$d->nome?></a>
                                <?php else : ?>
                                    <span><?=$d->nome?></span>
                                <?php endif ?>
                            </td>
                            <td><?= $d->unidade ?? "—" ?></td>
                            <td><?= $d->ultimo_valor !== null ? number_format((float)$d->ultimo_valor, 4, ",", ".") : "—" ?></td>
                            <td><?= $d->ultima_data ? date("d/m/Y", strtotime($d->ultima_data)) : "—" ?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]): ?>
                                            <a href="<?=$router->route("admin.indice.financeiro.editar", ["id" => $d->hash])?>" class="table-action-btn" data-tooltip="true" title="Editar / Histórico"><i class="uil-edit-alt"></i></a>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]): ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir" onclick="Delete('indices-financeiros/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="modal-indice-financeiro" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            <div class="modal-body"></div>
            <div class="modal-footer mt-3">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button type="submit" class="btn btn-primary" form="form-indice-financeiro"><i class="uil uil-check me-1"></i>Salvar</button>
            </div>
        </div>
    </div>
</div>

<?php $this->start("endScripts") ?>
<script src="<?=asset("admin/js/indice-financeiro-form.js")?>"></script>
<?php $this->end() ?>
