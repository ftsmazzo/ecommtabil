<?php $this->layout("app/template/base"); ?>

<?php $this->start("styles") ?>
<link rel="stylesheet" type="text/css" href="<?=asset("dist/dizuploader/dizuploader.min.css")?>">
<?php $this->end() ?>

<?php $this->start("page-buttons") ?>
    <?php if ($permissao["inserir"]): ?>
        <button type="button" class="btn btn-primary" data-modal-ajax="<?=$router->route("admin.cartao.bandeira.novo")?>" data-modal-target="#modal-cartao-bandeira" data-modal-title="Nova Bandeira"><i class="fa fa-plus fa-fw"></i> Nova Bandeira</button>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th style="width:60px"></th>
                        <th>Nome</th>
                        <th>Bandeira</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr data-record-id="<?=$d->hash?>">
                            <td class="text-center logo-cell">
                                <?php if ($d->logo) : ?>
                                    <img src="<?=storage()?>/media/cartao_bandeira/<?=$d->logo?>" alt="<?=$d->nome?>" style="height:25px;width:auto;object-fit:contain;">
                                <?php else : ?>
                                    <span class="text-muted"><i class="uil uil-image-slash"></i></span>
                                <?php endif ?>
                            </td>
                            <td>
                                <?php if ($permissao["editar"]): ?>
                                    <a class="edit" href="#" data-modal-ajax="<?=$router->route("admin.cartao.bandeira.editar", ["id" => $d->hash])?>" data-modal-target="#modal-cartao-bandeira" data-modal-title="Editar Bandeira"><?=$d->nome?></a>
                                <?php else : ?>
                                    <span><?=$d->nome?></span>
                                <?php endif ?>
                            </td>
                            <td><?=$d->bandeira?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]): ?>
                                            <button type="button" class="table-action-btn" data-tooltip="true" title="Editar" data-modal-ajax="<?=$router->route("admin.cartao.bandeira.editar", ["id" => $d->hash])?>" data-modal-target="#modal-cartao-bandeira" data-modal-title="Editar Bandeira"><i class="uil-edit-alt"></i></button>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]): ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir" onclick="Delete('cartoes/bandeiras/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="modal-cartao-bandeira" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            <div class="modal-body"></div>
            <div class="modal-footer mt-3">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button type="submit" class="btn btn-primary" form="form-cartao-bandeira"><i class="uil uil-check me-1"></i>Salvar</button>
            </div>
        </div>
    </div>
</div>

<?php $this->start("endScripts") ?>
<script type="text/javascript" src="<?=asset("dist/dizuploader/dizuploader.min.js")?>"></script>
<script src="<?=asset("admin/js/cartao-bandeira-form.js")?>"></script>
<?php $this->end() ?>
