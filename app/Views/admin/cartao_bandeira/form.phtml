<form id="form-cartao-bandeira" class="form-validate" action="<?=$url_action?>" method="POST" enctype="multipart/form-data">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($bandeira->id)) : ?>
        <input type="hidden" name="id" value="<?=$bandeira->hash()?>">
    <?php endif ?>

    <div class="mb-3">
        <label for="cb-nome" class="form-label" title="Campo Obrigatório">Nome <span class="text-danger">*</span></label>
        <input type="text" class="form-control" id="cb-nome" name="nome" placeholder="Ex: Visa Crédito" required value="<?=$bandeira->nome ?? ""?>">
    </div>

    <div class="mb-3">
        <label for="cb-bandeira" class="form-label" title="Campo Obrigatório">Bandeira <span class="text-danger">*</span></label>
        <input type="text" class="form-control" id="cb-bandeira" name="bandeira" placeholder="Ex: Visa, Mastercard, Elo" required value="<?=$bandeira->bandeira ?? ""?>">
    </div>

    <div class="mb-0">
        <label class="form-label">Logo</label>
        <?php if (!empty($bandeira->logo)) : ?>
            <div class="logo-preview-wrap mb-2" id="cb-logo-preview">
                <img src="<?=storage()?>/media/cartao_bandeira/<?=$bandeira->logo?>" alt="Logo atual" style="height:48px;width:auto;object-fit:contain;">
                <button type="button" class="btn btn-outline-danger btn-sm ms-2 btn-delete-logo"
                    data-delete-logo-url="<?=$router->route("admin.cartao.bandeira.delete.logo")?>"
                    data-id="<?=$bandeira->hash()?>"
                    data-csrf="<?=$csrf?>"
                    data-target="cb-logo-preview"
                    data-record-id="<?=$bandeira->hash()?>">
                    <i class="uil uil-times"></i> Remover logo
                </button>
            </div>
        <?php endif ?>
        <div>
            <input type="file" name="logo" id="cb-logo" data-extensions="jpg jpeg png gif webp svg">
        </div>
    </div>
</form>
