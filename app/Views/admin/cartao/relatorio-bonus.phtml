<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-buttons") ?>
    <a class="btn btn-light" href="<?=$router->route("admin.cartao.index")?>"><i class="uil uil-arrow-left me-1"></i> Voltar para Cartões</a>
<?php $this->end() ?>

<?php $this->start("endScripts") ?>
<script src="<?=asset("dist/datatables/plugins/sorting/datetime-br.js")?>"></script>
<script>
(function () {
    $("#table-relatorio-bonus").data("datatable-options", {
        order: [[0, "desc"]],
        columnDefs: [
            { targets: [0], type: "datetime-br" }
        ]
    });
}());
</script>
<?php $this->end() ?>

<div class="card mb-3">
    <div class="card-body">
        <form method="GET" action="<?=$router->route("admin.cartao.relatorio.bonus")?>">
            <div class="row g-3 align-items-end">
                <div class="col-sm-6 col-lg-3">
                    <label for="data_inicial" class="form-label">Data inicial</label>
                    <input type="date" class="form-control" id="data_inicial" name="data_inicial" value="<?=htmlspecialchars($filtros->data_inicial ?? '', ENT_QUOTES, 'UTF-8')?>">
                </div>
                <div class="col-sm-6 col-lg-3">
                    <label for="data_final" class="form-label">Data final</label>
                    <input type="date" class="form-control" id="data_final" name="data_final" value="<?=htmlspecialchars($filtros->data_final ?? '', ENT_QUOTES, 'UTF-8')?>">
                </div>
                <div class="col-sm-6 col-lg-3">
                    <label for="tipo" class="form-label">Tipo</label>
                    <select class="form-select" id="tipo" name="tipo">
                        <option value="">Todos</option>
                        <option value="CASHBACK" <?=($filtros->tipo ?? '') === 'CASHBACK' ? 'selected' : ''?>>Cashback</option>
                        <option value="FIDELIDADE" <?=($filtros->tipo ?? '') === 'FIDELIDADE' ? 'selected' : ''?>>Fidelidade</option>
                    </select>
                </div>
                <div class="col-sm-12 col-lg-3">
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary"><i class="uil uil-filter me-1"></i> Filtrar</button>
                        <a class="btn btn-light" href="<?=$router->route("admin.cartao.relatorio.bonus")?>"><i class="uil uil-times me-1"></i> Limpar</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

<div class="row g-3 mb-3">
    <div class="col-md-4">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small mb-1">Cashback concedido</div>
                <div class="h4 mb-1">R$ <?=moedabr((float) ($resumo->cashback_total ?? 0))?></div>
                <div class="small text-muted"><?=number_format((int) ($resumo->cashback_quantidade ?? 0), 0, ',', '.')?> lançamentos</div>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small mb-1">Fidelidade concedida</div>
                <div class="h4 mb-1">R$ <?=moedabr((float) ($resumo->fidelidade_total ?? 0))?></div>
                <div class="small text-muted"><?=number_format((int) ($resumo->fidelidade_quantidade ?? 0), 0, ',', '.')?> lançamentos</div>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small mb-1">Total concedido</div>
                <div class="h4 mb-1">R$ <?=moedabr((float) ($resumo->total_geral ?? 0))?></div>
                <div class="small text-muted"><?=number_format((int) ($resumo->quantidade_geral ?? 0), 0, ',', '.')?> lançamentos</div>
            </div>
        </div>
    </div>
</div>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table id="table-relatorio-bonus" class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Data</th>
                        <th>Tipo</th>
                        <th>Cartão</th>
                        <th>Cliente</th>
                        <th>Descrição</th>
                        <th>Valor</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach (($movimentacoes ?? []) as $mov) : ?>
                        <tr>
                            <td><?=$mov->data_print?></td>
                            <td><?=$mov->tipo_print?></td>
                            <td><span class="fw-semibold"><?=$mov->cartao_print?></span></td>
                            <td><?=$mov->cliente_print?></td>
                            <td><?=htmlspecialchars((string) ($mov->descricao_print ?? '-'), ENT_QUOTES, 'UTF-8')?></td>
                            <td data-order="<?=$mov->valor?>"><span class="fw-semibold text-success"><?=$mov->valor_print?></span></td>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
