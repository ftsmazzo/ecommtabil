<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
    <?php if (!empty($permissao["cartao"])): ?>
        <a class="btn btn-white btn-sm" href="<?=$router->route("admin.cartao.index")?>"><i class="uil uil-credit-card me-1"></i>Lista de Cartões</a>
    <?php endif ?>
<?php $this->end() ?>

<?php $this->start("page-buttons") ?>
    <?php if ($permissao["inserir"] && !empty($pode_criar)): ?>
        <button type="button" class="btn btn-primary" data-modal-ajax="<?=$router->route("admin.fidelidade.regra.novo")?>" data-modal-target="#modal-fidelidade-regra" data-modal-title="Nova Regra de Fidelidade"><i class="fa fa-plus fa-fw"></i> Nova Regra</button>
    <?php elseif ($permissao["inserir"]): ?>
        <button type="button" class="btn btn-primary cursor-not-allowed" disabled title="Ja existe uma regra vigente no momento"><i class="fa fa-plus fa-fw"></i> Nova Regra</button>
    <?php endif ?>
<?php $this->end() ?>

<?php if (!$pode_criar && !empty($regra_vigente)) : ?>
    <?php $temElegiveis = ($elegiveis ?? 0) > 0; ?>
    <div class="alert <?= $temElegiveis ? 'alert-warning' : 'alert-info' ?> d-flex align-items-center justify-content-between gap-3 flex-wrap mb-3">
        <div>
            <div><i class="uil uil-heart me-1"></i> Já existe uma regra vigente: <strong><?= htmlspecialchars((string) $regra_vigente->descricao, ENT_QUOTES, 'UTF-8') ?></strong></div>
            <div>Gatilho: <strong><?= $regra_vigente->gatilho_print ?? '-' ?></strong></div>
            <div>Bônus: <strong>R$ <?= number_format((float) $regra_vigente->valor_saldo, 2, ',', '.') ?></strong></div>
        </div>
        <div class="d-flex align-items-center gap-2 flex-shrink-0">
            <?php if ($temElegiveis) : ?>
                <span class="badge bg-warning text-dark fs-6">
                    <?= $elegiveis . ($elegiveis > 1 ? ' cartões elegíveis' : ' cartão elegível') ?>
                </span>
                <?php if (!empty($permissao['editar'])) : ?>
                <a href="<?= $router->route('admin.fidelidade.regra.aplicar.bonus', ['id' => $regra_vigente->id]) ?>"
                   class="btn btn-sm btn-warning"
                   onclick="ConfirmLink(this.href, 'Aplicar bônus de fidelidade a <?= $elegiveis ?> cartão(ões) elegível(eis)?'); return false;">
                    <i class="uil uil-award me-1"></i> Aplicar Bônus
                </a>
                <?php endif ?>
            <?php else : ?>
                <small class="text-muted">Nenhum cartão elegível no momento</small>
            <?php endif ?>
        </div>
    </div>
<?php endif ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Descrição</th>
                        <th>Gatilho</th>
                        <th>Bônus</th>
                        <th>Período</th>
                        <th>Ativo</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr>
                            <td>
                                <?php if ($permissao["editar"]): ?>
                                    <a class="edit" href="<?=$router->route("admin.fidelidade.regra.editar", ["id" => $d->id])?>" data-modal-ajax="<?=$router->route("admin.fidelidade.regra.editar", ["id" => $d->id])?>" data-modal-target="#modal-fidelidade-regra" data-modal-title="Editar Regra de Fidelidade"><?=$d->descricao?></a>
                                <?php else : ?>
                                    <span><?=$d->descricao?></span>
                                <?php endif ?>
                            </td>
                            <td><?=$d->gatilho_print?></td>
                            <td><?=$d->saldo_print?></td>
                            <td><?=$d->periodo_print?></td>
                            <td><?=$d->ativo_print?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]): ?>
                                            <button type="button" class="table-action-btn" data-tooltip="true" title="Editar" data-modal-ajax="<?=$router->route("admin.fidelidade.regra.editar", ["id" => $d->id])?>" data-modal-target="#modal-fidelidade-regra" data-modal-title="Editar Regra de Fidelidade"><i class="uil-edit-alt"></i></button>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]): ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir regra" onclick="Delete('fidelidade/regras/delete', '<?=$d->id?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="modal-fidelidade-regra" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            <div class="modal-body"></div>
            <div class="modal-footer mt-3">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button type="submit" class="btn btn-primary" form="form-fidelidade-regra"><i class="uil uil-check me-1"></i>Salvar</button>
            </div>
        </div>
    </div>
</div>

<?php $this->start("endScripts") ?>
<script src="<?=asset("admin/js/fidelidade-regra-form.js")?>"></script>
<?php $this->end() ?>
