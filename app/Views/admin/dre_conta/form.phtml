<form id="form-dre-conta" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if ($conta) : ?>
        <input type="hidden" name="id" value="<?=$conta->id?>">
    <?php endif ?>

    <input type="hidden" name="tipo_demonstrativo" value="<?=htmlspecialchars($tipo, ENT_QUOTES, "UTF-8")?>">
    <input type="hidden" name="tipo" value="<?=htmlspecialchars($tipoConta, ENT_QUOTES, "UTF-8")?>">
    <?php if ($pai) : ?>
        <input type="hidden" name="id_pai" value="<?=$pai->id?>">
    <?php endif ?>

    <?php if ($pai) : ?>
        <div class="alert alert-light border mb-3 py-2 small">
            <i class="uil uil-corner-down-right me-1"></i>
            Subconta de <strong><?=htmlspecialchars($pai->nome, ENT_QUOTES, "UTF-8")?></strong>
            <span class="text-muted">(<?=$pai->codigo?>)</span>
        </div>
    <?php endif ?>

    <div class="mb-3">
        <label for="dre-nome" class="form-label">Descrição <span class="text-danger">*</span></label>
        <input type="text" class="form-control" id="dre-nome" name="nome"
            placeholder="Ex: Receita Bruta"
            value="<?=htmlspecialchars($conta->nome ?? "", ENT_QUOTES, "UTF-8")?>"
            required autofocus>
    </div>

    <div class="mb-3">
        <label class="form-label">Movimentação</label>
        <div class="d-flex gap-3">
            <div class="form-check">
                <input class="form-check-input" type="radio" name="natureza" id="nat-aumenta" value="aumenta"
                    <?=(!$conta || $conta->natureza === "aumenta") ? "checked" : ""?>>
                <label class="form-check-label" for="nat-aumenta">
                    <span class="text-success fw-semibold">(A)</span> Aumenta
                </label>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="natureza" id="nat-diminui" value="diminui"
                    <?=($conta && $conta->natureza === "diminui") ? "checked" : ""?>>
                <label class="form-check-label" for="nat-diminui">
                    <span class="text-danger fw-semibold">(D)</span> Diminui
                </label>
            </div>
        </div>
    </div>
</form>
