<?php $this->layout("app/template/base"); ?>

<?php $this->start("styles") ?>
<?php $this->end() ?>

<?php $this->start("startScripts") ?>
<script src="<?= asset("/dist/webpush/webpush.js")?>"></script>
<script>
    WebPush.init({
        publicKey: "<?= $vapidKey ?>",
        autoSubscribe: false,
        userToken: '<?= $userToken ?>'
    });
</script>
<?php $this->end() ?>

<div class="row g-3 mb-4">
    <div class="col-12 col-md-3">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small">Empresas</div>
                <div class="fw-bold fs-3"><?=number_format((int) $qtdEmpresas, 0, ",", ".")?></div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-3">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small">Projetos</div>
                <div class="fw-bold fs-3"><?=number_format((int) $qtdProjetos, 0, ",", ".")?></div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-3">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small">Lançamentos importados</div>
                <div class="fw-bold fs-3"><?=number_format((int) ($totais->total_lancamentos ?? 0), 0, ",", ".")?></div>
                <div class="text-muted small"><?=number_format((int) ($totais->projetos_com_dado ?? 0), 0, ",", ".")?> projeto(s) com dado</div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-3">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small">Soma dos lançamentos</div>
                <div class="fw-bold fs-4">R$ <?=number_format((float) ($totais->valor_total ?? 0), 2, ",", ".")?></div>
            </div>
        </div>
    </div>
</div>

<?php if (!empty($recentes)) : ?>
    <h6 class="text-muted text-uppercase small mb-2">Vistos recentemente</h6>
    <div class="d-flex flex-wrap gap-2 mb-4">
        <?php foreach ($recentes as $rec) : ?>
            <a class="btn btn-outline-secondary btn-sm" href="<?=htmlspecialchars((string) $rec["url"], ENT_QUOTES, "UTF-8")?>">
                <?=htmlspecialchars((string) $rec["label"], ENT_QUOTES, "UTF-8")?>
            </a>
        <?php endforeach ?>
    </div>
<?php endif ?>

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <span class="fw-semibold">Projetos</span>
        <a class="btn btn-sm btn-primary" href="<?=$router->route("admin.projeto.index")?>">Ver todos</a>
    </div>
    <div class="table-responsive">
        <table class="table table-sm table-hover mb-0 align-middle">
            <thead class="table-light">
                <tr>
                    <th>Projeto</th>
                    <th>Empresa</th>
                    <th class="text-end">Lançamentos</th>
                    <th class="text-end">Valor</th>
                </tr>
            </thead>
            <tbody>
                <?php if (empty($projetos)) : ?>
                    <tr>
                        <td colspan="4" class="text-center text-muted py-4">Nenhum projeto ainda.</td>
                    </tr>
                <?php else : ?>
                    <?php foreach ($projetos as $p) : ?>
                        <tr style="cursor:pointer" onclick="window.location='<?=$p->abrir_url?>'">
                            <td><?=htmlspecialchars((string) ($p->nome ?: "-"), ENT_QUOTES, "UTF-8")?></td>
                            <td><?=htmlspecialchars((string) $p->empresa_print, ENT_QUOTES, "UTF-8")?></td>
                            <td class="text-end"><?=number_format((int) ($p->resumo->total_lancamentos ?? 0), 0, ",", ".")?></td>
                            <td class="text-end">R$ <?=number_format((float) ($p->resumo->valor_total ?? 0), 2, ",", ".")?></td>
                        </tr>
                    <?php endforeach ?>
                <?php endif ?>
            </tbody>
        </table>
    </div>
</div>
