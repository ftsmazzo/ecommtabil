<?php $this->layout("app/template/base"); ?>

<?php $this->start("styles") ?>
<style>
    @keyframes icon-spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
    .icon-spin { animation: icon-spin 0.8s linear infinite; display: inline-block; }
</style>
<?php $this->end() ?>

<?php $this->start("page-buttons") ?>
    <form id="form-igpm-sincronizar" action="<?=$router->route("admin.configuracao.igpm.sincronizar")?>" method="POST">
        <input type="hidden" name="csrf" value="<?=$csrf?>">
        <button type="submit" class="btn btn-primary" id="btn-sincronizar-igpm">
            <i class="uil uil-sync"></i> Sincronizar com o Banco Central
        </button>
    </form>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table" data-datatable-options='{"order":[[0,"desc"],[1,"desc"]]}'>
                <thead>
                    <tr>
                        <th>Ano</th>
                        <th>Mês</th>
                        <th>Variação no Mês</th>
                        <th>Acumulado no Ano</th>
                        <th>Atualizado em</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($registros)): ?>
                        <tr>
                            <td colspan="5" class="text-center text-muted py-4">
                                Nenhum dado sincronizado ainda. Clique em <strong>Sincronizar com o Banco Central</strong> para importar a série histórica.
                            </td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($registros as $r): ?>
                            <tr>
                                <td><?=$r->ano?></td>
                                <td><?=str_pad((string) $r->mes, 2, "0", STR_PAD_LEFT)?></td>
                                <td><?= $r->variacao_mensal !== null ? number_format((float) $r->variacao_mensal, 2, ",", ".") . "%" : "—" ?></td>
                                <td><?=number_format((float) $r->valor_acumulado, 2, ",", ".")?>%</td>
                                <td><?= $r->updated_at ? date("d/m/Y H:i", strtotime($r->updated_at)) : date("d/m/Y H:i", strtotime($r->created_at)) ?></td>
                            </tr>
                        <?php endforeach ?>
                    <?php endif ?>
                </tbody>
            </table>
        </div>
    </div>
</div>

<?php $this->start("endScripts") ?>
<script>
document.getElementById("form-igpm-sincronizar").addEventListener("submit", function () {
    const btn = document.getElementById("btn-sincronizar-igpm");
    btn.disabled = true;
    btn.querySelector("i").classList.add("icon-spin");
});
</script>
<?php $this->end() ?>
