<?php $this->layout("app/template/base"); ?>

<?php $this->start("endScripts") ?>
<script src="<?=asset("dist/table2excel/jquery.table2excel.js")?>"></script>
<script src="<?=asset("dist/datatables/plugins/sorting/datetime-br.js")?>"></script>
<script type="text/javascript" src="<?=asset("admin/js/usuario-historico.js")?>"></script>
<?php $this->end() ?>

<?php $this->start("page-actions") ?>
    <button type="button" class="btn btn-green" id="exportXls">
        <i class="uil uil-export"></i> Exportar
    </button>
    <a href="<?=$url_back?>" class="btn btn-white">
        <i class="uil uil-left"></i> Voltar
    </a>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <?php if ($usuario): ?>
            <div class="info-bar mb-3">
                <?php if (!empty($usuario->foto)): ?>
                    <div class="info-bar-avatar">
                        <img src="<?=media('usuarios', $usuario->foto)?>" alt="<?=$usuario->login?>">
                    </div>
                <?php else: ?>
                    <div class="info-bar-icon bg-primary">
                        <i class="uil uil-user"></i>
                    </div>
                <?php endif ?>
                <div class="info-bar-copy">
                    <small>Historico do usuario</small>
                    <strong><?=$usuario->login?></strong>
                </div>
            </div>
        <?php endif ?>

        <div class="crud-subsection history-filter-bar mb-3">
            <div class="row g-2 align-items-end">
                <div class="col-md-4 col-lg-3">
                    <label class="form-label"><i class="uil uil-calendar-alt"></i> Data/Hora Inicial</label>
                    <input type="datetime-local" class="form-control" id="filter-data-inicial">
                </div>
                <div class="col-md-4 col-lg-3">
                    <label class="form-label"><i class="uil uil-calendar-alt"></i> Data/Hora Final</label>
                    <input type="datetime-local" class="form-control" id="filter-data-final">
                </div>
                <div class="col-md-4 col-lg-2">
                    <button type="button" class="btn btn-outline-secondary w-100" id="filter-data-limpar">
                        <i class="uil uil-times"></i> Limpar
                    </button>
                </div>
                <div class="col-lg-4">
                    <div class="history-counter" id="history-counter">
                        <span class="history-counter-label">Registros encontrados</span>
                        <strong class="history-counter-value"><?=count($historico)?></strong>
                    </div>
                </div>
            </div>
        </div>

        <div class="table-responsive">
            <table id="table-historico" class="table table-striped table-hover table-sm crud-table">
                <thead>
                    <tr>
                        <th>Data</th>
                        <?php if (!$usuario): ?>
                            <th>Usuario</th>
                        <?php endif ?>
                        <th class="d-none d-md-table-cell">IP</th>
                        <th class="d-none d-lg-table-cell">Local</th>
                        <th>Acao</th>
                        <th>Status</th>
                        <th class="d-none d-lg-table-cell">Motivo</th>
                        <th>Sistema</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($historico as $d): ?>
                        <tr>
                            <td><?=$d->data?></td>
                            <?php if (!$usuario): ?>
                                <td>
                                    <div class="cell-title">
                                        <span><?=$d->login_usuario ?: "-"?></span>
                                        <small><?=$d->login ?: "Sem login informado"?></small>
                                    </div>
                                </td>
                            <?php endif ?>
                            <td class="d-none d-md-table-cell"><?=$d->ip ?: "-"?></td>
                            <td class="d-none d-lg-table-cell"><?=$d->local ?: "-"?></td>
                            <td><?=$d->acao ?: "-"?></td>
                            <td><?=$d->status_print?></td>
                            <td class="d-none d-lg-table-cell"><?=$d->motivo ?: "-"?></td>
                            <td>
                                <span title="<?=$d->sistema?>"><?=$d->sis ?: "-"?></span>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
