<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<?php $this->start("page-buttons") ?>
    <?php if ($permissao["inserir"]): ?>
        <a class="btn btn-primary" href="<?=$router->route("admin.usuario.novo")?>"><i class="fa-solid fa-plus fa-fw"></i> Novo Usuário</a>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Cargo</th>
                        <th class="d-none d-lg-table-cell">Perfil</th>
                        <th class="d-none d-md-table-cell">Login</th>
                        <th class="d-none d-md-table-cell">Status</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr data-id="<?=$d->id?>">
                            <td>
                                <div class="cell-title">
                                    <?php if ($permissao["editar"]) : ?>
                                        <a class="edit" href="<?=$router->route("admin.usuario.editar", ["id" => $d->id])?>"><?=$d->nome?></a>
                                    <?php else : ?>
                                        <span><?=$d->nome?></span>
                                    <?php endif ?>
                                    <small><?=$d->email ?: ($d->cargo ?: "Sem informacoes complementares")?></small>
                                </div>
                            </td>
                            <td><?=$d->cargo ?: "-"?></td>
                            <td class="d-none d-lg-table-cell"><?=$d->perfil_print?></td>
                            <td class="d-none d-md-table-cell"><?=$d->login?></td>
                            <td class="d-none d-md-table-cell"><?=$d->status_print?></td>
                            <td class="text-end actions-column">
                                <div class="table-actions">
                                    <?php if ($permissao["editar"]): ?>
                                        <a class="table-action-btn" data-tooltip="true" title="Editar" href="<?=$router->route("admin.usuario.editar", ["id" => $d->id])?>"><i class="uil-edit-alt"></i></a>
                                    <?php endif ?>
                                    <button type="button" class="table-action-btn table-action-danger" <?=$d->disabled?> <?php if (empty($d->disabled)) : ?>data-tooltip="true"<?php endif ?> title="<?=$d->title?>" <?=$d->action?>><i class="uil uil-trash"></i></button>
                                </div>
                            </td>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
